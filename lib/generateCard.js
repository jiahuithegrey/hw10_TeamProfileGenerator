switch(role) {
    case manager:
      // code block
      icon: "fa-mug-hot";
      SpeicalName: "Office Number";
      SpecialValue: "role.getOffice.Number";

        break;
    case engineer:
      // code block
        break;
    case intern:
    // code block
        break;
    default:
      // code block
  }